addPythonModule(
    NESTED_NAME
        z5py
    SOURCES
        z5py.cxx
        dataset.cxx
        util.cxx
    LIBRRARIES
        ${COMPRESSION_LIBRARIES} 
        ${Boost_FILESYSTEM_LIBRARY}    
        ${Boost_SYSTEM_LIBRARY}    
        ${CMAKE_THREAD_LIBS_INIT}
)
