addPythonModule(
    NESTED_NAME
        z5py
    SOURCES
        z5py.cxx
        dataset.cxx
        util.cxx
    LIBRRARIES
        ${COMPRESSION_LIBRARIES} 
        ${FILESYSTEM_LIBRARIES}
        ${CMAKE_THREAD_LIBS_INIT}
)
